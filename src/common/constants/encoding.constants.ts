/**
 * Character encoding constants for text processing operations.
 * Centralized to ensure consistency across the application.
 */

/**
 * UTF-8 encoding constant for text operations.
 * Used for file I/O, HTTP responses, and text decoding.
 */
export const ENCODING_UTF8 = "utf8" as const;
