/**
 * Common constants used across the application.
 * These are foundational values that should not depend on other application layers.
 */
export const commonConstants = {
  /**
   * UTF-8 encoding constant for file I/O operations
   */
  UTF8_ENCODING: "utf8",
} as const;
